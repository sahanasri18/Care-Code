# ===== CareCode frontend environment template =====
# Copy to .env and adjust for local development.

# Base URL of the CareCode API (used by axios; proxy handles /api in dev)
VITE_API_URL=

# Public origin used to build absolute asset URLs (QR images, photos)
VITE_PUBLIC_BASE_URL=
